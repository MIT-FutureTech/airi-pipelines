# Baseline results

Contents are gitignored. These are outputs of the proof-of-concept this pipeline
replaces, kept so a port can be diffed against what it is meant to reproduce.

| File | What it is |
|---|---|
| `poc_results_rows1-730_agora-matched.csv` | The PoC's full 730-document run, `google/gemini-3.5-flash-lite`, 2026-07-28, after `match_agora.py` added the five AGORA match columns. Documented in `../docs/poc-report.md`. |
